# Task 0001 — Generate Ragdoll on rig-001

This is the planned first code-development task. Its input scenario is prepared; generator implementation has not been authorized or attempted in this record.

## Requested tool outcome

Implement the Generate Ragdoll behavior described in [the tool specification](../../../docs/first-tool.md): select the supported rig, generate whole-body ragdoll physics, and drive the character with a keyable animation-to-physics blend. Generation and baking are separate buttons. This task covers generation and its blend; baking is a subsequent task.

## User instruction that authorized fixture preparation

> Ok lets create the first test scenario

The assistant chose the concrete Idle/floor test, 24-fps schedule, 1.8-m scale, 0.08-m clearance, and candidate measurement limits to make that request reviewable. These are recorded scenario choices, not verbatim user requirements or evidence of approved generator code.

## Starting input and expected result

Use [scenario.md](scenario.md) and [input/manifest.json](input/manifest.json). Success must come from the implemented tool run on that input. Preserve the input and original rig; save output in a new run directory. Attach proposed code changes, approval, implementation revision, corrections, and observed results when the first attempt starts.
